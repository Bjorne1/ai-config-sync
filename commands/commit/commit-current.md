---
description: 提交当前对话中的代码（仅限本次对话修改的文件）
---

# 提交当前对话代码变更
仅提交本次对话中修改的代码并推送。

## 逻辑判断：
回顾本次对话的上下文：
1. 如果这是本次对话的**首次**提交：提交信息应涵盖本次对话实现的**核心功能**。
2. 如果此前**已经生成过**提交命令：本次提交信息应仅聚焦于**最近一次修改或修复**（增量变更）。

## Git 提交规范：
使用约定式格式：<type>(<scope>): <主题>，其中 type = feat|fix|docs|style|refactor|test|chore|perf。主题：最多50字符，使用祈使语气（如"添加"而非"已添加"）。简单改动：仅需一行提交信息。复杂改动：添加正文说明改动内容和原因（每行72字符）。如涉及不同功能，应拆分为多个提交。除了专业术语外，使用中文注释。

## 排除文件（不要提交）：
- `package.json`
- `LoginInfo.java`